# futsal — 偽ピヴォ パターン集（引き継ぎ）

## これは何か
フットサルの「偽ピヴォ（フォルソ・ピヴォ）」と関連パターンを、単一HTMLのアニメーションで見せるページ。
自分用＋チーム共有用。GitHub Pages で公開する。

- 公開先: https://fkktr.github.io/futsal/ （リポジトリ: fkktr/futsal）
- ファイルは `index.html` 1枚。外部依存なし（CDN・フォント読み込みなし）。ビルド不要。

## まずやること
1. `index.html` をリポジトリ直下に置いて main に push
2. リポジトリを Public にする（無料プランの Pages は Public のみ）
3. Settings → Pages → Deploy from a branch / main / root

## ファイル構造（index.html）
- `<style>`: 淡い地色（#FAFAF7 / コート #EEF3EA）、攻撃 `--atk` #2F6F73、守備 `--def` #B5654A、狙うスペース `--space` #E9C46A。
  濃い背景は使わない（ユーザーの好み）。
- `<svg viewBox="0 0 400 520">`: ハーフコート、ゴールは上（y=20）。`#overlays` `#players` `#ball` は JS が動的生成。
- `<script>`: `PATTERNS` 配列 ＋ 共通エンジン。パターン追加は配列に1要素足すだけ。

## パターンのデータ構造
```js
{
  tab:   'タブ表示名',
  title: 'H2', sub: '一行説明',
  players: {...GK, ...MAN, ...ATK},   // id: [ラベル, 初期x, 初期y]。ATK に含まれる id が攻撃色
  K: { id: [[t,x,y], ...], ball: [[t,x,y], ...] },  // キーフレーム。省略した選手は初期位置で固定
  overlays: [ { tag:'ellipse'|'path'|'text', attrs:{...}, text?, arrow?:true,
                in:[fadeIn開始, 終了], out:[fadeOut開始, 終了], op?:最大不透明度 } ],
  caps: [ [開始秒, 'ボタン名', 'キャプションHTML'], ... ],  // ボタンも自動生成
  end: 総秒数
}
```
共通プリセット: `ATK`（F/A1/A2/P）, `GK`, `MAN`（マンツー D1–D4）, `SQ`（2-2: DB1/DB2 後ろ, DF1/DF2 前）。
1-2-1 プレスだけ players をインライン定義している。

座標の目安: 攻撃初期 F(200,470) A1(80,330) A2(320,330) P(200,130)。マンツー D1(200,100) はピヴォのゴール側。

## 現在の7パターン
1. 対マンツー：引き出してアラが裏へ（⑤詰め・残り あり）
2. 対マンツー：ついてこない → ピヴォが前を向く
3. 対スクエア：片側を引き出してフィクソ上がり（⑤あり）
4. 対スクエア：釘付け → ピヴォがライン間のアラへ落とす
5. 対マンツー：サイド落ち → パラレラ → セグンド（⑤あり）
6. 対スクエア：ダブルピヴォ（⑤あり）
7. 対1-2-1プレス：降りて逃げ道 → 剥がして前進

## 設計上の約束事（ユーザーと合意済み）
- 分岐の軸は「マーカーが出てくるか」。1↔2、3↔4 は対。
- 落とした人・運んだ人は前に行かない。前に行くのは打つ人と詰める人の2人まで。残り2人は中央（点線楕円で表示）。
- 4 はピヴォの落としをフィクソに戻さず、ライン間のアラが受ける（修正済み。戻さない）。
- 1 の逆サイド展開は振り直しではなく死角からの斜め走り。降りる位置が広すぎる場合は 5 の同サイド。
- 6 は前に4人いるのでカウンターに最も弱い。ピヴォが下がる矢印を明示している。

## 未着手・候補
- 2, 4, 7 にも ⑤（詰めと残り）を入れる（1,3,5,6 と同じ形式）
- 「ゾーンの受け渡しの瞬間を突く」パターン。動きよりタイミングなので、受け渡しの瞬間だけスロー再生などの見せ方が要る
- 再生速度の切替は未実装（`prefers-reduced-motion` で0.6倍になるだけ）

## 検証
```sh
node -e "const fs=require('fs');const s=fs.readFileSync('index.html','utf8');const m=s.match(/const PATTERNS = \[[\s\S]*?\n  \}\];/)[0];const ATK={},GK={},MAN={},SQ={};eval(m.replace('const PATTERNS','var PATTERNS'));console.log(PATTERNS.length)"
```
ブラウザで開いて7タブすべて再生し、ボールが選手に追従していることを確認。
